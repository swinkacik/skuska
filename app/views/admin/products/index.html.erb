<h1>Admin produktov</h1>

<p>
  <%= link_to "Pridať produkt", new_admin_product_path, class: "btn btn-primary" %>
</p>

<table class="table">

  <thead>
    <tr>
      <th>Nazov</th>
      <th>Popis</th>
      <th>cena</th>
    </tr>
  </thead>

  <tbody>

    <% @products.each do |product| %>
    <tr>
      <td><%= product.name %></td>
      <td><%= product.describtion %></td>
      <td><%= product.price %></td>
      <td><%= product.published %></td>
      <td><%= link_to "Upravit", edit_admin_product_path(product) %></td>
    </tr>
    <% end %>

  </tbody>
</table>
